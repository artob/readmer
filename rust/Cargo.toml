# See: https://doc.rust-lang.org/cargo/reference/manifest.html

[package]
name = "readmer"
version = "0.0.0"
authors = ["Arto Bendiken"]
edition = "2024"
#rust-version = "1.85"
description = "Compose README.md files from templates."
#documentation = "https://docs.rs/readmer"
readme = true
homepage = "https://readmer.dev"
repository = "https://github.com/artob/readmer"
license = "Unlicense"
keywords = ["readme", "markdown"]
categories = ["command-line-utilities", "development-tools", "template-engine", "text-processing"]
publish = true

[features]
default = ["all", "std"]
all = []
alloc = []
std = ["alloc"]
unstable = []

[dependencies]

# Preview locally with: RUSTDOCFLAGS="--cfg docsrs" cargo +nightly doc --all-features
[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]
